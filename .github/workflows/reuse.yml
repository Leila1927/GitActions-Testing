
name: Reuse Build
run-name: Reusable build
on:
  workflow_call:
jobs:
  Premier-Job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World!!"
  Build:
    runs-on:  ubuntu-latest
    steps:
        - uses: Actions/checkout@v3 #downloads a copy of repo on the runner 
        - name: Set up JDK 11 
          uses: Actions/setup-java@v3
          with: 
            java-version: '11'
            distribution: 'adopt'